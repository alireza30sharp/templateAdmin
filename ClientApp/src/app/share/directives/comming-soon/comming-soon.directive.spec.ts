import { CommingSoonDirective } from './comming-soon.directive';

describe('CommingSoonDirective', () => {
  it('should create an instance', () => {
    const directive = new CommingSoonDirective();
    expect(directive).toBeTruthy();
  });
});
